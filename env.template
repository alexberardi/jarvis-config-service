# =============================================================================
# jarvis-config-service - Environment Template
# =============================================================================
# Copy to .env and fill in values
# Port: 7700
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# DB_URL: used when running in Docker (host.docker.internal resolves to host machine)
# MIGRATIONS_DATABASE_URL: used for local dev and migrations (localhost)
DB_URL=postgresql://postgres:postgres@host.docker.internal:5432/jarvis_config
MIGRATIONS_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jarvis_config

# -----------------------------------------------------------------------------
# ADMIN
# -----------------------------------------------------------------------------
# Token for write operations (POST/PUT/DELETE)
# Generate with: openssl rand -hex 32
JARVIS_CONFIG_ADMIN_TOKEN=__SET_ME__

# -----------------------------------------------------------------------------
# SERVER
# -----------------------------------------------------------------------------
JARVIS_CONFIG_PORT=7700
HOST=0.0.0.0

# Health check timeout in seconds
HEALTH_CHECK_TIMEOUT=5.0
