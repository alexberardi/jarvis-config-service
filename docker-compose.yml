services:
  jarvis-config:
    build: .
    ports:
      - "${JARVIS_CONFIG_PORT:-8013}:${JARVIS_CONFIG_PORT:-8013}"
    env_file: .env
    environment:
      - DATABASE_URL=${DB_URL}
    extra_hosts:
      - "${DOCKER_HOST_ALIAS:-host.docker.internal}:host-gateway"
    restart: unless-stopped
