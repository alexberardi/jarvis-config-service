services:
  jarvis-config:
    build: .
    ports:
      - "8013:8013"
    env_file: .env
    extra_hosts:
      - "${DOCKER_HOST_ALIAS:-host.docker.internal}:host-gateway"
    restart: unless-stopped
